/**
 * Source authority — re-exported for the Edge runtime from the single canonical
 * implementation. Keeping one file means a retailer cannot start counting as a
 * manufacturer because two copies drifted apart.
 */
export * from '../../../src/features/product-intelligence/sourceAuthority.ts';
