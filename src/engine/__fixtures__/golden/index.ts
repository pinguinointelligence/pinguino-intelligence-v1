/**
 * Golden test recipes — synthetic reference mixes asserting the spec §9 bands.
 *
 * Deliberately EMPTY in Step 4B: golden recipes are authored together with the
 * calculation pipeline (4C+), and no verified production recipes are invented.
 * Engine-spec Appendix A remains illustrative / calibration-pending and is NOT
 * seeded here until MyGelato calibration confirms the coefficient set.
 */
import type { ActiveRecipeFixture } from '../schema';

export const GOLDEN_FIXTURES: readonly ActiveRecipeFixture[] = [];
