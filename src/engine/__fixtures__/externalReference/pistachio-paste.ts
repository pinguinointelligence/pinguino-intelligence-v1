import type { CalibrationFixture } from '../schema';

/** external calibration fixture (spec §16) — awaiting real ingredient data. */
export const pistachioPaste: CalibrationFixture = {
  kind: 'ingredient',
  name: 'pistachio-paste',
  status: 'pending',
  notes: 'Pure pistachio paste composition with external reference expected values.',
};
