# src/stores — client state (Zustand)

`recipeStore.ts` (working recipe — engine recalcs on every change) and `sessionStore.ts`
(plan/access level, demo persistence). Created with the Studio build step (Masterplan §9).
