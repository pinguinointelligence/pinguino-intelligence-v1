# src/data — local seed data

`baseIngredients.ts` (~30 PINGÜINO base ingredients, literature-based, confidence-scored) and
`demoLimits.ts` (demo ingredient subset + limits). Phase 1 runs entirely on this local data —
no backend (Masterplan §9, §20).
