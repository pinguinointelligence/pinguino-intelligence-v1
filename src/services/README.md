# src/services — server integration (Phase 2+)

`supabase.ts`, `recipes.ts`, `ingredients.ts`, `ai.ts` (Edge Function calls). Nothing here in
Phase 1 — the demo runs entirely on local data (Masterplan §9, §17, §18).
