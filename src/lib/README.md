# src/lib — utilities

`units.ts` (g/kg/L conversion + category density assumptions, always user-overridable),
`format.ts` (display rounding: 0.1 g / 0.1 %), `currency.ts` (EUR default, configurable).
Masterplan §7 (Page 1), §12.10.
