# Scan Core — Phase 1 status (internal closeout, 2026-09-04 evening)

Branch `claude/scan-core-phase-0` @ 742a9d61 · PR #155 DRAFT (not merged, not FINAL, not OWNER ACCEPTED, not integrated into HOME). Tests: 21 files / 128 passing (`npx vitest run src/scan-core src/scan-lab`), `tsc --noEmit` clean, eslint clean.

## A. Owner architecture list — where each item lives
| # | item | status | code | tests |
|---|---|---|---|---|
| 1 | persistent physical barcode track ids | EXISTED | `track.ts` `Track.id = t<n>` (never reused in a session; `resetTrackIds()` for tests), SORT-style association with damped constant-velocity prediction, LOST after 500 ms, purged after 2 s | `track.test.ts` (identity at hand speed, LOST/re-create) |
| 2 | track-scoped evidence memory | EXISTED | `Track.evidence[]` capped at 120: `valid_read`, `invalid_hypothesis`, `error_geometry`, `quality`, each with provenance (`source`, `format`, `rawText`, `error`) | `track.test.ts` (bounded evidence, invalid hypotheses never change confirmation) |
| 3 | best-frame / best-ROI bounded memory | EXISTED | `Track.best` (4 keys: sharpest, contrast, largestModule, leastTilt), `retryFrames()` | `track.test.ts` |
| 4 | track-scoped confirmation | EXISTED | one `Confirmation` per track; fast lane (2 agreeing reads ≤ 400 ms, lineCount ≥ 4, module ≥ 2 px) + consensus lane (4 agreeing, 2:1 margin, 1.5 s window) | `confirmation.test.ts`, `engine.test.ts` (two codes confirm independently) |
| 5 | two-code evidence isolation | EXTENDED 2026-09-04 | `engine.attributeRescue()`: a full-frame rescue read attaches only to an unambiguous owner (single live track, or the one track that already read those digits), else retained in `engine.unattributedReads` and never confirmed; `Tracker` 2-opt swap guard after greedy association | `isolation.test.ts` (rescue never attributed to the primary while another live track could own it; attributed to the track that read the digits; crossing codes keep ids) |
| 6 | safe failed-read evidence retention | EXISTED | checksum-invalid / partial / decoder-error results stored as `invalid_hypothesis` / `error_geometry`; only `checksumValid` reads reach `pushRead` | `engine.test.ts`, `track.test.ts` |
| 7 | actual decoder symbology propagation | EXTENDED 2026-09-04 | agreement key = digits + `formatFromDecoder(format)`; reads of different symbologies never agree; `ConfirmationState.format` + `mixedFormats`; observation carries `barcode.format` (from the decoder string, never digit count), `barcode.rawValue`, and a `mixed_formats` reason | `isolation.test.ts`, `stateMachine.test.ts` ("comes from the decoder string, never from digit count") |
| 8 | rectified evidence cannot confirm alone (§17) | EXISTED | fast lane excludes rectified reads; consensus requires an independent non-rectified read of the same digits+format | `confirmation.test.ts` (§17 six rectified aliases) |
| 9 | per-track decode escalation | EXTENDED 2026-09-04 | `Track.escalationLevel()` 0 cheap · 1 harder/rectified when tilted · 2 retry best retained frames · 3 rescue-eligible; derived from consecutive misses, reset by a valid read, fresh for a returning code; exported in `TrackDecisionRecord.escalation` | `isolation.test.ts`, `engine.test.ts` (harder after two misses, retry against best frames) |
| 10 | state machine / debounce / HOLD / timeout | EXTENDED 2026-09-04 | SEARCHING/FOUND/READING/HOLD/COMPLETE/LOST, 3-frame debounce, FOUND > 1.5 s → auto action before guidance, blocker after 1 s of the same blocking guidance, LOST → SEARCHING after 2 s; NEW `STATE.readingTimeoutMs = 4000` (PROVISIONAL) → `timedOut` + blocker + `hold_steady`, re-armed per track; `FrameDecisionRecord.timedOut` | `stateMachine.test.ts`, `isolation.test.ts` |
| 11 | capability profiles | EXISTED | `tiers.ts` `classifyTier`, `TIER_BUDGETS`, `budgetFor` (pure data) | `tiers.test.ts` |

Type changes are additive (`rawValue`, `format`/`rawText` on reads and evidence, `escalation`, `timedOut`, `unattributedReads`); the harness (`src/scan-lab/baseline/worker/scanCoreLane.ts`) compiles unchanged.

## B. Physical evidence reconciled today
- RERUN matrix 6/6 + 1 bonus session received through the tunnel, verified (ZIP, SHA-256 in both copies, session id, device line, scene count), parsed, ledger: `reports/scan-core-phase-0/RERUN_2026-09-04_LEDGER.md`.
- Baseline confirmed two wrong values on two phones (`0602002492786`, `8622262492786` for `7622201492786`); the `0602…` alias recurred on a second phone → systematic decoder alias at ≈ 25 cm.
- Phase 1 policy replay over the RERUN corpus: **57 confirmations, 0 wrong**; both wrong-value scenes confirmed correctly (`P1_POLICY_REPLAY_2026-09-04.md`; morning corpus preserved as `…_morning-corpus.md`).
- iPhone Safari-engine sessions never decode at 12 cm (5/5 runs over two days), Chrome iOS decodes 12 cm only after ~4 s: near-focus limit of the web capture path, not the PWA mode and not today's light.

## C. MEASURE FIRST — needs owner/device evidence (unchanged by today's code)
1. Note10+ P1 probe (session `NOTE10-P1-PROBE-2026-09-04`): rgba_buffer vs image_bitmap vs video_frame loops, resolution-switch and zoom stall/focus/exposure recovery, then scene 26 in Scan Core v0. Decides the acquisition policy and the Android throughput fix.
2. iOS near-range: macro/lens handoff (deferred owner decision) or a "move back to 20 cm" guidance rule; the cross-day table is the evidence base.
3. `STATE.readingTimeoutMs = 4000` and the LOW_MEDIUM path cost (Realme 25 cm took 5.4 s under the policy) — tune only with probe evidence.
4. Desktop/MacBook 12-scene audit — owner physically present.

## D. Next steps (for the next session / Opus)
- Ingest the P1 probe bundle when it lands (`tools/ingest.mjs` or the collector does it), parse, add to the ledger, reconcile the transfer-path and probe measurements against §9 of `P1_ADAPTIVE_DESIGN_2026-09-04.md`.
- Feed the two alias values and the six-rectified-alias case into a regression fixture for `policyReplay`/`confirmation` (values are in the ledger).
- Keep PR #155 DRAFT; no HOME integration until "SCAN CORE ACCEPTED — INTEGRATE INTO HOME".
