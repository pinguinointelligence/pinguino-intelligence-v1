# FINAL CORE CLOSURE LEDGER — 2026-07-25

## Starting baseline
Staging `a55f5fc` (bundle NHh7acy2) → integrated wave → **staging `623a2ce`, bundle `index-BCQAOdui.js`** (content-verified: „Dopasowanie techniczne" ×3, `hard_residual_violations`, „Niedostępny"). Vercel deployment confirmed via new bundle after push. main untouched. Interruption note: a monthly spend-limit outage killed Agent M mid-run and the first science workflow; M was preserved via WIP commit and resumed to completion; a git maintenance crash corrupted the MAIN checkout index and a stale worktree index and dropped the artifacts commit off the branch — repaired (index rebuilt, `22cdc8d` re-merged, EOL noise normalized, stale index removed).

## Agent 3 / acceptance addendum (branch fix/acceptance-addendum @ 1a11965) — ALL FOUR CLOSED
1. **T9 gate**: constrained + `iteration_cap` ⇒ `impossible_under_constraints`, never a preview; nearest-feasible lock 894.9 g (hardened bisection probe); deterministic switch-to-Sorbet alternative; door-enforced (forged-preview test). `iteration_cap` can never label best-achievable.
2. **Score split**: `recipeTechnicalFit` — all native bands in range ⇒ exactly 10/10 headline („Dopasowanie techniczne"); engine blend never leaks into the headline; flavor/cost separate labeled dimensions; provisional profiles never validated 10/10. §15.1 pins deliberately superseded (documented).
3. **Hard residuals**: door trustlessly recomputes band provenance of the PROPOSED result; `hardSafe=false` ⇒ diagnostic preview only, `hard_residual_violations` block naming metrics; T14/T19 no longer applicable (recipe byte-untouched proof). Soft/provisional residuals stay applicable.
4. **MAX/RANGE**: real defect found — grams-lock staging outranked the range branch, degrading ranges to exact. Fixed: T12 milk max-500 → 380 g (<500, 2→1 violations); milk max-800 → 670 g (7→0); exact-500 stays byte-500; range object survives preview+apply round-trip.
Extra: local-correction ADDs filtered by canonical exclusions (Agent R handoff) — excluded ingredients never return via local route.

## Monitor parity + one-screen workbench (Agent M @ 6d612eb)
Zero page scroll verified in a REAL browser at 1366×768 / 1440×900 / 1920×1080 (scrollHeight == viewport, 8-row recipe); workbar 63 px, settings 53 px, editor 62% (rows 50 px, internal scroll) + LIVE Monitor 38%; Przelicz overlay 660 px; tab row removed with all 9 routes in the hamburger (+proMachine); `/pro/monitor` = workbench with panel focused; mobile bottom-sheet with identical content. Full B1 parity inventory executable (`monitorParity.test.tsx`): summary layer (N/10, states, six axes, readiness, warning) + every historical module collapsible (temperature behavior, sugars, water/frozen phase, fats, proteins, solids, stabilization + provenance sentence, alcohol, nutrition, costs incl. restored „Koszt partii", PI corrections, iteration diagnostics) + owner diagnostics red ADVANCED. Two live defects found&fixed in-browser (sr-only caption extending scroll 128 px; 122 px workbar). Screenshots could not composite (browser pane) — DOM measurements recorded in the M ledger §1a; visual pass = owner morning item.

## Refresh-required bug (Agent R @ d828df2)
First differing pre/post-refresh field: `exclusions` (`["cream_30","smp","dextrose"]` vs `[]`) — removal auto-excluded roles so the toolbox could not refill them; refresh cleared unpersisted exclusions. Fix: **removal ≠ exclusion** (C2; `markIngredientUnavailable` is the only exclusion source, per-row „Niedostępny"), orphan §17 constraints dropped synchronously in the same setState, ONE draftRevision bump per material edit, canonical snapshot on click, 20 no-refresh cycles deterministic (15-test suite).

## Engine T1–T20 (post-fix artifacts, regenerated on the final tree)
`docs/engine-validation/ENGINE_AUTHENTICITY_TESTS.json/.csv` — ENGINE 0.4.0 / CONFIG 0.7.0, 10× determinism byte-identical. Changed outcomes per the addendum: T9 HONEST-IMPOSSIBLE (no preview); T14/T19 applyAllowed=false (diagnostic, hard native ice); T12 true range (fixed point below max); T17 AUTHENTIC-OPTIMAL — raw engine blend 88.17 recorded, public technical fit = 10/10 via `recipeTechnicalFit` (pinned by OverallScoreCard tests). Zero SUSPECTED-FAKE.

## Gates (integrated tree 623a2ce)
`tsc -b` 0 · `eslint` 0 errors · **5186/5186 tests (384 files)** · build ✓ · zero merge conflicts across addendum→R→M.

## Design preservation
Logo hash-locked ✓ · dark charcoal contrast ✓ · simplified primary path ✓ (now one-screen) · no route/menu/function deleted (route-inventory tests) ✓ · red review zone + pink TESTOWE provenance preserved ✓.

## Remaining science (separate from software)
`STABILIZER SCIENCE — BLOCKED_SCIENCE` (engine has no stabilizer metric; multi-stabilizer selector design + dose forensics in progress — research agents running; owner ratification will follow the updated proposal). Fruit-family bands + Protein bands = owner decisions per ENGINE_ONLY_ARCHITECTURE_PROPOSAL.md.

## Owner verification (only true owner-visible items)
Authenticated click-run of: minimal Gelato, Strawberry-900 (expect impossible + suggestion), Milk exact/max, Sorbet inulin-0 (diagnostic, no Apply), immediate multi-remove→recalc without refresh, one-screen zero-scroll feel, Monitor completeness, Apply/Undo/Save/reopen, visual/screenshot pass.
