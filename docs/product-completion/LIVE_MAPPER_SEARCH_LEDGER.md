# LIVE COMPLETE MAPPER SEARCH — completion ledger

**Status:** `LIVE COMPLETE MAPPER SEARCH — DEPLOYED, AWAITING OWNER VERIFICATION`
**Date:** 2026-07-23 · Scope: search architecture only. No Engine/optimizer/Mapper-value/flag/status change; no migration.

## Owner failures reproduced (mechanically, from the traced path)
- **pineapple → only FORTEFRUTTO paste + FANTA beverage, no `PI-ING-000390`:** the one-time catalogue load ordered `ingredient_name_display ASC` with NO `.range()` → Supabase/PostgREST silently capped it at **1,000 of 2,070** rows. Row #1000 = „LACTOSE DRY · Powder". Everything alphabetically after „L" NEVER reached the browser: `PINEAPPLE · Fresh Fruit` (position **1,497**), `STRAWBERRIES · Fresh Fruit` (1,842), `RASPBERRIES` (1,614), `WHOLE MILK` (2,032), every `MILK …` row (1,209–1,229). FORTEFRUTTO (F) and FANTA (F) were inside the first 1,000 — exactly the owner's symptom.
- **milk → buttermilk/condensed/coconut/chocolate but no ordinary milk:** same cap — BUTTERMILK (B), COCONUT MILK (C), CONDENSED MILK (C) are < „L"; `MILK 3.5%`/`MILK 2%`/`WHOLE MILK` are > „L" and absent. Additionally the ranking had no concept-category affinity, so even present look-alikes outranked semantics.

## Root cause
- **One-time catalogue load existed:** yes — `useIngredientLibrary` ran `listEngineApprovedIngredients()` once (react-query key `['pi-base-ingredients']`, staleTime 5 min) and every keystroke filtered that in-memory array locally.
- **Actual loaded row count:** 1,000 (PostgREST default max rows; 2,070 qualify).
- **1,000-row cap:** yes — the first exact loss point for ordinary milk AND all the missing Fresh Fruit rows (loss at the network request, before mapper/filter/ranking).
- **Cache lifetime:** 5 minutes stale + session-long in-memory array → newly added catalogue data was invisible mid-session.
- **Compatibility filters:** none removed rows (no such filter exists in the picker path).
- **Ranking defects:** no concept awareness (milk ≻ milk chocolate) — fixed with category affinity.

## New architecture
- **Service:** `searchEngineApprovedIngredients(rawQuery, {limit, signal})` ([ingredients.ts](src/services/ingredients.ts)) — per settled query, one PostgREST request: `is_active ∧ approved_for_engines ∧ (AND per token)(OR of alias-term ilikes over 6 SAFE columns)`; ordered display+id; **limit 200** (covers the largest verified concept family — milk = 95 — whole, so client ranking sees every candidate; the cap can never truncate a concept family). SAFE payload only: id/display/internal/brand/category/subcategory — **no PAC/POD/composition** (pinned by test). Full scientific row resolves ON SELECTION by exact stable id via `getIngredientById` (RLS).
- **Hook:** `useIngredientSearch` — debounce **250 ms**; queryKey `['ingredient-search', normalizedQuery, limit]` (an old response can never overwrite a newer query); react-query aborts the previous request on key change (`abortSignal` wired to PostgREST); `staleTime` **15 s** + `refetchOnMount: 'always'` → reopening the picker / repeating a query after a backend change refetches in-session; `hasMore` + „Pokaż więcej wyników" pagination (limit grows per-query); empty query fetches NOTHING.
- **Picker:** [ServerIngredientPicker.tsx](src/features/ingredient-builder/ServerIngredientPicker.tsx) — live results ranked (below), form label shown; stale-add protection: the selection is keyed to the settled query, `Dodaj składnik` disabled until the CURRENT response settles, and the add resolves the row fresh by exact id — `banana` → replace with `milk` → Add can never insert banana.
- **Ranking** ([ingredientSearch.ts](src/features/ingredient-builder/ingredientSearch.ts)): (1) concept-category affinity — controlled alias families now carry the Mapper categories the concept lives in (milk/mleko→dairy, fruits→fruit, herbs→botanical, sugars→sweetener), so ordinary milk outranks MILK CHOCOLATE/COCONUT MILK/base mixes/pastes WITHOUT hiding them; (2) name-match quality (exact-prefix > leading word > word > substring > SKU-only — a code fragment can never top a semantic match); (3) natural form (fresh/plain milk → frozen → puree → juice/cream → … → paste → powder → aroma → beverage); (4) alphabetical, stable. „skimmed milk powder" ranks the powder record first (form text participates in semantics).
- **Library:** `useIngredientLibrary` no longer preloads ANY catalogue — it loads only „My Products" + their exact matched reference rows (`listIngredientsByIds`); `serverSearchLibrary()` marks the canonical Pro shape. Demo/non-Pro keep the local 12-ingredient preview catalog (never fetch PI Base). No localStorage/IndexedDB anywhere in the path (pinned).
- **Consolidation:** ONE search implementation. The old local-filter picker remains only as the demo/fallback view over the 12 preview ingredients; the alias/normalization/ranking layer is shared by both (one canonical module).

## Complete coverage (full audit: [MAPPER_SEARCH_COVERAGE.md](docs/product-completion/MAPPER_SEARCH_COVERAGE.md))
2,070 active searchable rows · **discoverable by exact ID: 2,070 (100%)** · **by exact name: 2,070 (100%)** (the single pre-fix gap — `PI-ING-001462` „Polska Róża", non-ASCII display token absent from internal — closed via the rose alias family) · missing IDs: none · honestly-absent natural forms listed (mango fresh, whole lemon/orange fresh, rosemary/thyme herbs, vanilla extract, raw ananas Blocked) — reported, NOT invented.

## Dynamic freshness (performed on verified staging tunab — reversible, reversed)
`zzqatest` → 0 rows → inserted QA row `PI-ING-QA0001` → the SAME app filter returns it (no redeploy) → deleted → 0 rows again. No scientific value of any real row touched. In-browser same-session confirmation on the served authenticated picker = owner step (agent cannot enter credentials).

## Completed
Files: [ingredients.ts](src/services/ingredients.ts) (search service + `listIngredientsByIds` + `isIngredientBackendConfigured`), [ingredientSearch.ts](src/features/ingredient-builder/ingredientSearch.ts) (alias families + categories, `conceptCategoriesFor`, `nameMatchQuality`, `buildSearchTermGroups`, `rankSearchHits`), [useIngredientSearch.ts](src/features/ingredient-builder/useIngredientSearch.ts), [ServerIngredientPicker.tsx](src/features/ingredient-builder/ServerIngredientPicker.tsx), [useIngredientLibrary.ts](src/features/ingredient-builder/useIngredientLibrary.ts), [ingredientLibrary.ts](src/features/ingredient-builder/ingredientLibrary.ts), [IngredientBuilder.tsx](src/features/ingredient-builder/IngredientBuilder.tsx), [en.ts](src/copy/en.ts). No migration/RPC needed (existing table + RLS suffice; filters stay inside PostgREST). Tests: NEW [serverSearchRanking.test.ts](src/features/ingredient-builder/serverSearchRanking.test.ts) (16 — real staging row fixtures: milk/mleko ordinary-first incl. buttermilk/condensed/coconut/chocolate/base-mix ordering, milk 3.5%/mleko 3,5% exact, skimmed-milk-powder-first, pineapple/ananas/piña fresh-first, basil/bazylia + mint/mięta fresh-herb-first, SKU-never-tops, safe payload) + NEW [liveSearchContract.test.ts](src/features/ingredient-builder/liveSearchContract.test.ts) (12 — per-query request with alias OR-groups, AND per token, limit, EMPTY-query-fetches-nothing, abortable, exact-id reach, safe columns, no-preload pin, freshness-contract pin, no-storage pin, stale-add pin, builder mount pin). Existing suites green. **Gate: 4,679 tests / 345 files · ESLint 0 errors · tsc ✓ · build ✓.** Staging deployment + served-bundle proof: final report.

## Not completed
- Served AUTHENTICATED browser network/UI proof (typing milk/pineapple/… as Pro and the in-app freshness re-query) — **AWAITING OWNER**: the picker is Pro-gated and credential entry is disallowed for the agent; local dev cannot substitute (local .env points at the old prod project, and anon RLS returns no base-table rows). Every layer below the login wall is proven: physical rows → view/RLS (returns 2,070 for pro@pro.com) → the EXACT request filter (SQL replay: milk 95 / pineapple 13 / strawberry 48 / basil 1 / vanilla 44 candidates, all within one page) → mapper → ranking (unit, real row fixtures) → served bundle contains the new path.
- Honestly-absent natural records (mango fresh, vanilla extract, rosemary/thyme, raw ananas Blocked) — data curation, not search.

## Regression proof
Shell/menu, workbar, save/version, delete, Mapper identity (stable `PI-ING-*` into RecipeInput on add — same `ingredientRowToEngineIngredient` seam), Engine (untouched), Monitor (same RecipeInput), Pro entitlement (search enabled only for Pro off-demo; demo keeps preview catalog), duplicate-recalculation P0 suite (18/18), Polish aliases + form labels + fresh ranking (all prior suites green) — full run 4,679/4,679.

## Owner acceptance (staging, Pro, /pro/recipe picker)
`milk` → MILK 3.5% / WHOLE MILK first (buttermilk/condensed lower; chocolate/coconut/base mixes far lower) · `pineapple` / `ananas` → `PI-ING-000390 · PINEAPPLE · Fresh Fruit · Świeży owoc` first · `strawberry`/`truskawka` → STRAWBERRIES · Fresh Fruit · `banana` → BANANA · Fresh Fruit · `basil`/`bazylia` → BASIL · Botanical · Fresh · `vanilla`/`wanilia` → vanilla forms · type `banana`, replace with `milk`, click Dodaj → milk added, never banana · freshness: have a new approved row added (e.g. by Nicolas) and repeat its query in the SAME session → it appears.
