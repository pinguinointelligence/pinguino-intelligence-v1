# PRZELICZ Z PI — REAL AUTO-BALANCE — completion ledger

**Status:** `PRZELICZ Z PI — REAL AUTO-BALANCE CONNECTED ON STAGING, AWAITING OWNER VERIFICATION`
**Date:** 2026-07-23 · Scope: orchestration/wiring/classification only. Engine science untouched (TARGET_BANDS / ICE_ANCHOR_ROWS / PAC/POD / Mapper / weights / bounds unchanged; ENGINE 0.4.0 / CONFIG 0.7.0 pinned by test).

## Root cause (Phase 1–3 trace, proven with the real solver)
- **Was the solver called?** YES — exactly once per click. The chain: `Przelicz z PI` → `startRecalc` (ProWorkspacePage) → `createOptimizePreview()` (constraintStudioStore) → `buildOptimizePreview` (applyPipeline) → `proposeAutoFix` → `proposeCorrections` (the REAL canonical engine solver). No Monitor-direction dependency existed; the button was NOT a no-op or validation-only path.
- **Which function?** The canonical correction solver — but as a SINGLE-SHOT, violation-driven corrector with NO batch objective: (1) `target_batch_grams` equality was not part of any objective — a 254 g or 6 g draft against a 1000 g target had no mechanism pulling it to batch (batch restore ran only AFTER a successful solver proposal); (2) one propose→apply round only — a catastrophically unbalanced recipe whose FIRST correction round yields no applicable action dead-ended immediately. Reproduced: the all-1 g recipe (6 g total, 8 band violations) → `no_proposal`.
- **Adjustable variables:** NOT the problem — all unlocked lines were adjustable (`GŁÓWNY` never implied a lock; the lock report proved 0 locked). Candidate count was non-zero.
- **Why every input returned the same message:** every `no_proposal` with zero locks was classified `no_active_locks` and rendered the one fixed sentence („Solver nie znalazł możliwej korekty… przyczyną nie są blokady") with no proof attached — indistinguishable across causes.

## Completed
- **Auto-balance orchestration** ([applyPipeline.ts](src/features/constraint-studio/applyPipeline.ts) `buildOptimizePreview`) — composes ONLY approved mechanisms, no new math: (1) **batch reconciliation FIRST** via §17.4 `rescaleBatchToTarget` (locked grams byte-exact, per-100 g concentrations preserved) — batch-total equality is now part of the DEFAULT objective, so a 1 g draft becomes a solvable 1000 g recipe; (2) violations on the batch-true recipe → none and nothing changed → `already_clean`; (3) otherwise **iterate** the canonical solver (`proposeAutoFix`/`applyAutoFix`, each round Golden-Middle-verified by the engine itself) to a fixed point (≤4 rounds), with canonical-identity merge + batch restore after every round; (4) anything changed → verified Preview carrying `autoBalance: {batchRescaled, solverRounds}`; nothing changed → `no_proposal` WITH PROOF (`solverInvocations`, `violatedMetrics`). Neutral Monitor state never yields an empty objective (the default objective = bands + batch + locks + minimal change via the solver's own ranking).
- **Contract (Phase 7, documented):** the solver path returns a COMPLETE next RecipeInput; Apply stays the atomic replacement through `commitPreview` (duplicate + batch invariants from the previous P0 remain in force at the door).
- **Error classification (Phase 9):** zero-lock `no_proposal` now classifies as **`optimizer_no_solution`** with the proof — message „PI przeliczyło recepturę (solver uruchomiony N ×), ale nie znalazło bezpiecznej korekty w zatwierdzonych zakresach. Parametry poza zakresem: …" + PL metric labels ([constraintStudioCopy.ts](src/features/constraint-studio/constraintStudioCopy.ts), [recalcDiagnosis.ts](src/features/constraint-studio/recalcDiagnosis.ts), [ProRecalcPanel.tsx](src/features/pro-core/ProRecalcPanel.tsx)); „Receptura nie została zmieniona." still always appended; all-locked message unchanged; lock-blame still requires ≥1 verified lock. `already_clean` → the owner's exact sentence „Receptura znajduje się już w zatwierdzonym zakresie. PI nie proponuje zmian."
- **Tests** — NEW [autoBalance.test.ts](src/features/constraint-studio/autoBalance.test.ts) (15, real solver): the owner's mutilated 254 g case AND the literal all-1 g (6 g) case return REAL previews at 1000±0.1 g at −11/−12/−13 with `batchRescaled` proof and no duplicate identities; GŁÓWNY main-line at 1 g still recalculates; near-balanced ~999.91 g → already_clean or valid preview (never unclassified); re-run → classified (clean / batch-true preview / PROVEN fixed point); one lock byte-exact (`Object.is`); all-locked → explicit message; store path preview→apply→1000 g→no duplicates; Undo byte-exact; failed build never mutates; save/reopen preserved; failure message carries „solver uruchomiony N ×" + metrics; ENGINE/CONFIG pinned. Updated pins: [recalcDiagnosis.test.ts](src/features/constraint-studio/recalcDiagnosis.test.ts) (zero-lock → proven optimizer_no_solution), [recalcDuplication.test.ts](src/features/constraint-studio/recalcDuplication.test.ts) (the balance may add ONE genuinely-new toolbox line — uniqueness + no-growth-across-cycles asserted instead of a fixed row count). **Gate: 4,708 tests / 347 files PASS · ESLint 0 errors · tsc ✓ · build ✓.**
- **Phase-1 evidence (all-1 g reproduction):** ingredients UI=RecipeInput=6 · locks 0 · adjustable 6 · target 1000 g · total 6 g · residual −994 g · optimizer called YES ·solver called YES (invocations ≥1) · old result `no_proposal` → generic sentence · new result **preview, total 1000 g**.

## Not completed
- Served AUTHENTICATED owner click-through (Test 1 + Test 2 on staging) — AWAITING OWNER (Pro-gated; agent cannot log in). The exact store path the button drives is proven end-to-end with the real solver.
- A recipe at the honest FIXED POINT (batch-true but with residual unimprovable violations, e.g. immediately re-running after a full balance) correctly reports the proven `optimizer_no_solution` with the residual metric list — this is honest behavior, not a defect; deeper convergence would require new solver capabilities (= science change, out of scope).

## Regression proof
Engine science — untouched, versions pinned. Ingredient search + grouping/labels — untouched (suites green). Stable IDs — preserved (merge keeps line identity). Save/version — pinned. Workbar — same store calls. Locks — byte-exact. Duplicate invariant — still enforced at the Apply door (18/18 + updated pins green). Full suite 4,708/4,708.

## Owner acceptance (staging, Pro, /pro/recipe)
**Test 1:** wklej recepturę ~1000 g → `Przelicz z PI` → „Receptura znajduje się już w zatwierdzonym zakresie…" LUB realny Podgląd. **Test 2:** ustaw składniki na 1 g → `Przelicz z PI` → realne gramatury, suma 1000 g, Podgląd → Zastosuj (bez duplikatów) → Cofnij przywraca wartości 1 g.
